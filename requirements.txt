# TTS API - Dépendances Python

# Framework Web (ancien - Gradio)
flask>=3.0.0
flask-cors>=4.0.0
gradio>=4.12.0

# Framework Web (nouveau - Timeline Studio)
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
python-multipart>=0.0.6

# TTS et Machine Learning (laisse TTS gérer les versions de numpy/scipy)
TTS==0.22.0
torch==2.1.2
torchaudio==2.1.2

# Pour Windows GPU (optionnel - décommenter si vous avez un GPU NVIDIA)
# --extra-index-url https://download.pytorch.org/whl/cu121
# torch==2.1.2+cu121
# torchaudio==2.1.2+cu121
